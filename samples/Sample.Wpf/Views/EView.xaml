<UserControl x:Class="Sample.Wpf.Views.EView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Sample.Wpf.Views"
             xmlns:an="https://github.com/NeverMorewd/AsyncNavigation"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <DockPanel LastChildFill="True">
        <Border
        HorizontalAlignment="Stretch"
        Background="DarkOrange"
        BorderBrush="LemonChiffon"
        BorderThickness="1"
        DockPanel.Dock="Top">
            <StackPanel>
                <TextBlock Text="{Binding Name}" />
                <TextBlock>
				InstanceNumber:
				<Run
                    FontSize="15"
                    FontWeight="Bold"
                    Foreground="Blue"
                    Text="{Binding InstanceNumber, Mode=OneWay}" />
                </TextBlock>
                <Button Style="{StaticResource UnloadView}"/>
                <Button Style="{StaticResource CloseDialog}"/>
            </StackPanel>
        </Border>
    </DockPanel>
</UserControl>
