<Window
    x:Class="Sample.Wpf.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:an="https://github.com/NeverMorewd/AsyncNavigation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Sample.Wpf"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="MainWindow"
    Width="800"
    Height="450"
    WindowState="Maximized"
    mc:Ignorable="d">
    <Grid RowDefinitions="*,30">
        <DockPanel LastChildFill="True">
            <StackPanel DockPanel.Dock="Right">
                <StackPanel
                    Margin="0,4,0,4"
                    VerticalAlignment="Center"
                    Orientation="Horizontal">
                    <Label VerticalAlignment="Center" Content="Search:" />
                    <ComboBox
                        Name="SearchBox"
                        Width="245"
                        Height="35"
                        Margin="3,0,0,0"
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Center"
                        ItemsSource="{Binding Views}"
                        SelectedItem="{Binding SelectedView}" />
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <Button Style="{StaticResource GoBack}" />
                    <Button Style="{StaticResource GoForward}" />
                </StackPanel>
                <Button
                    Content="ToLightView"
                    Style="{StaticResource NavigationButton}"
                    Tag="LightView" />
                <Button
                    Content="ToHeavyView"
                    Style="{StaticResource NavigationButton}"
                    Tag="HeavyView" />
                <Button
                    Content="ToHeavyView:Delay"
                    Style="{StaticResource NavigationButton}"
                    Tag="HeavyView:Delay" />
                <Button
                    Content="ToLightView:New"
                    Style="{StaticResource NavigationButton}"
                    Tag="LightView:New" />
                <Button
                    Content="ToHeavyView:Error"
                    Style="{StaticResource NavigationButton}"
                    Tag="HeavyView:Error" />
                <Button
                    Content="ToLightView:Forget;New"
                    Style="{StaticResource NavigationAndForget}"
                    Tag="LightView:New" />
                <Button
                    Content="ToHeavyView:Forget;Delay"
                    Style="{StaticResource NavigationAndForget}"
                    Tag="HeavyView:Delay" />
                <Button
                    Content="ToItemsRegionView"
                    Style="{StaticResource NavigationButton}"
                    Tag="ItemsRegionView" />
                <Button
                    Content="ToTabRegionView"
                    Style="{StaticResource NavigationButton}"
                    Tag="TabRegionView" />
                <Button
                    Content="ToChildContentRegionView"
                    Style="{StaticResource NavigationButton}"
                    Tag="ChildContentRegionView" />
                <Button
                    Content="ToListBoxRegionView"
                    Style="{StaticResource NavigationButton}"
                    Tag="ListBoxRegionView" />
                <Button
                    Content="ToLightView:Dialog.Show"
                    Style="{StaticResource DialogShow}"
                    Tag="LightView" />
                <Button
                    Content="ToHeavyView:Dialog.Show"
                    Style="{StaticResource DialogShow}"
                    Tag="HeavyView" />
                <Button
                    Content="ToLightView:Dialog.ShowDialog"
                    Style="{StaticResource DialogShowDialog}"
                    Tag="LightView" />
                <Button
                    Content="ToHeavyView:Dialog.AsyncShowDialog"
                    Style="{StaticResource DialogShowDialogAsync}"
                    Tag="HeavyView" />
                <Button
                    Content="ToLightView:Dialog.ShowDialogWithCancelling"
                    Style="{StaticResource DialogShowDialogAsyncWithCancelling}"
                    Tag="LightView" />
                <Button
                    Content="ToLightView:Window.Show"
                    Style="{StaticResource WindowShow}"
                    Tag="AWindow" />
                <Button
                    Command="{Binding CollectCommand}"
                    Content="Collect"
                    Tag="" />
            </StackPanel>
            <ContentControl an:RegionManager.RegionName="MainRegion" />
        </DockPanel>
        <TextBlock
            Grid.Row="1"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Text="{Binding FooterText}" />
    </Grid>
</Window>
